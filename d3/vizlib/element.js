
var element = (function() {

  // all elements
  var id;  // a unique id for this element

  // all elements except line
  var sp = {x, y};
  var pos = {x, y};

  // svg line elements
  var sp = {x1, y1, x2, y2};
  var pos = {x1, y1, x2, y2};

  // non-text elements
  var fill;
  var fill_opacity;
  var stroke;
  var stroke_width;
  var stroke_opacity;

  // svg text elements
  var val;
  var font;
  var font_size;
  var text_anchor;

  // svg rect elements
  var width;
  var height;

  // svg circle elements
  var radius;

  return {

  }

})();
